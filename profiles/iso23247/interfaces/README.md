# ISO 23247 Interface Bindings

The versioned transport profiles are stored under `profiles/interfaces/{mqtt,opcua,rest}` and are referenced here rather than duplicated. Network semantics are represented independently through `networkType`.
